Check to include MAthJax only if use probably add a test on page.mathjax before to include and remove global config at true in order to just use local setting mathjax : true in Page or Post
Add Reserach Gate Link with same structure of other
